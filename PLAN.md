# Public demo scope

Ship a portable, sanitized version of the tested launch concierge: local work inbox → fresh selected memory → two real specialist agents → five files → separate structural validation → review.

Included: source, pinned dependencies, offline fixtures, configurable local server, bounded subscription adapter, actual Jev contracts, public project links and setup.

Excluded: private tenant data, runtime receipts, auth, personal account identities, original native app, copied vendor repositories and third-party film binaries. Next work is adapting operator-selected sources and testing the configured live path on another machine.
