# Observed verification · September 21, 2026 PDT

An actual local MacBook Pro run, inspected after completion, established:

- A new local work event triggered Digital George.
- Research Synth retrieved fresh Cognee graph context, including selected public X writing excerpts with original source URLs. The excerpts were inside the context actually supplied to the model.
- Bright Data retrieved the public Genie repository. Meta Muse returned an actual error page; the trace explicitly identified it as unusable product evidence.
- Proof Synth selected existing public project evidence and saved a project card.
- Digital George saved a launch brief, proof kit, LinkedIn post, four-post X thread and unsent introduction.
- Ten model responses produced ten validated Jev-contract advisory receipts. All identified Codex subscription reasoning, shadow mode, no applied action, and no genuine Jev-model inference.
- A separate Docker container exited successfully after checking all five artifacts. It had no network and read-only mounts.
- The original X posts contained 212, 232, 154 and 246 Unicode characters including numbering, whitespace and literal URLs. These are codepoint counts, not a claim to implement X's complete weighted-length algorithm.

A subsequent human/host review identified stale next-action wording that asked for another end-to-end task even though the run had completed. A tracked post-run review corrected the next action to reviewing one finished post and reran structural validation. This review was distinct from the earlier model generation.

Private run identifiers, raw memory, account details, local addresses, credentials and receipts are intentionally not published. This is an observed summary, not independently verifiable public access to a user's private tenant. Offline fixture tests cover the real Strands tool protocol, three-agent handoffs, fixed URLs, receipt identity and invalid-action rejection without making vendor/model calls.
