Digital George is a bounded launch concierge prototype inside SYNTHOS: a local work inbox triggers fresh Cognee memory, Bright Data research, real Strands specialist agents and five finished launch drafts.

Observed live run: ten subscription model calls, ten validated Jev-contract advisory receipts, two specialist handoffs, five saved files and a separate passing Docker structural check. No social posting or paid model API fallback. Jev contracts use Codex subscription judgments; genuine Jev-model inference is not claimed.

The 87-second, 1080p H.264/AAC demonstration recording is attached as `Digital-George-SYNTHOS.mp4` (29,528,556 bytes). Its GitHub SHA-256 matches the reviewed local file. The sanitized source excludes private memory, credentials, account identities and raw runtime receipts. See the README and STARTUP for scope and configuration.
